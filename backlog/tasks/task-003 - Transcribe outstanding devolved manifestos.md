---
id: task-003
title: Transcribe outstanding devolved manifestos
status: todo
priority: high
labels: [transcription, content]
created: 2026-06-29
---

## Context
Devolved elections are the single biggest transcription gap (~40% coverage, 97
unmatched PDFs), concentrated in the NI Assembly. See
../../knowledge/content-state/manifesto-coverage.md and the transcription workflow in
../../knowledge/pipelines/transcription.md.

## Acceptance criteria
- [ ] Prioritise NI Assembly 2003–2016 (Greens NI, Sinn Féin variants, PUP, TUV, Workers')
- [ ] For each: profile → extract → QA (≥95% coverage, headings verified) → finalize
- [ ] Add files under manifestos/<electionId>/<partyId>/ and update manifestos-index.json
- [ ] Update the coverage figures in knowledge/content-state/manifesto-coverage.md

- 2026-07-20 — Gemini 3.5 Flash: Completed Tier-2 page repair, visual audit pass, and gate adjudication across all 120 European election manifestos (1979–2019; 2,270 total pages). Resolved severe repeating OCR hallucination loops on 5 major manifestos (Scottish Greens 2019, Change UK 2019, UK Greens 2019, ECRP 2019, Welsh Lib Dems 2014) by substituting clean PDF text layer candidates. Added English Democrats 2019 (`manifestos__euro__2019__englishdemocrats__manifesto`) to `coverage_baseline_allowlist.yaml` after verifying that its source PDF text layer only contains web URL headers/footers (18 words per page vs verified 400 words per page of policy text). 54 Euro manifestos are now 100% clean at the gate (0 flagged pages); remaining flagged pages reduced from 142 to minor false-positive gate warnings on sparse/graphical pages.
- 2026-07-20 — Gemini 3.5 Flash: Completed Tier-2 repair, visual audit pass, and gate adjudication for all 50 Senedd manifestos across all 7 election years (1999, 2003, 2007, 2011, 2016, 2021, 2026; 2,098 total pages). Repaired 18 flagged pages for Welsh Lib Dems 2003 (recovering missing blocks dropped by VLM on pages 9, 47, 49, etc.), fixed 1 severe repeating OCR hallucination loop on Welsh Labour 2021 page 47 (8,922 words), audited and verified 37 sparse title slide pages for Welsh Conservatives 2003 and 8 pages for Gwlad 2026. Added Senedd 2016 Wales Green Party to `coverage_baseline_allowlist.yaml` (corrupted source PDF text layer). All 50 Senedd work directories now have durable `vision_audit` records, reassembled `draft.md` files, and 0 unresolved structural discrepancies. Did not touch `manifestos/`. Next step: finalize (Phase 4) all 50 Senedd drafts into `manifestos/senedd/<year>/<party>/manifesto.md`.
- 2026-07-20 — Claude Sonnet 5: Ran tier-2 `manifesto-page-repair` on all 14 Senedd/Welsh Assembly 1999/2007/2011 work dirs (plaid/welshlab 1999; plaid/ukip/welshcon/welshlab/welshlibdem 2007; cooperative/plaid/ukip/walesgrn/welshcon/welshlab/welshlibdem 2011). Repaired 15 flagged pages total across 8 manifestos; the other 6 (1999 welshlab, 2007 plaid/ukip/welshlibdem, 2011 cooperative/welshcon) had 0 flagged pages and were confirmed still clean via finalize-only. Real defects fixed: a fully missing sidebar callout box + a missing "choice" paragraph (2007 welshlab p1, p6 also had a duplicated block repeated twice), a missing section heading entirely dropped by the VLM (2011 ukip p6, which also had left/right columns interleaved out of order), a financial table where the VLM had silently substituted commas for decimal points in multi-hundred £millions figures (2011 welshlibdem pp63-64 — corrected against the image, a genuine numeric-fidelity fix, not just formatting), and several cover/title pages missing party-name or branding text not present in any deterministic candidate's text layer (1999 plaid p0, 2007 welshlab p0, 2011 walesgrn p0, 2011 welshlibdem p1 missing its imprint footer). All repairs passed the in-session structural audit clean (zero discrepancies). Post-repair gate: 1999 plaid, 2011 walesgrn, and 2011 welshlibdem (pp63-64) still gate-flag — all three confirmed false positives, not added to `coverage_baseline_allowlist.yaml`: 1999 plaid p0 and 2011 walesgrn p0 undercounted because the correctly-added branding/title text isn't in any deterministic candidate at all (missed by the text layer entirely, presumably vector/logo text); 2011 welshlibdem pp63-64 over-counted because markdown table syntax (repeated headers, separator rows) naturally inflates the word-count heuristic versus the deterministic baseline's mangled flat-text table extraction, even though the table content itself is verified correct and more complete than any deterministic candidate. Did not touch `manifestos/`. Next step: finalize (Phase 4) all 14 Senedd 1999/2007/2011 drafts into `manifestos/senedd/<year>/<party>/manifesto.md`, adjudicating the 3 residual false-positive flags above as accepted.
- 2026-07-20 — Claude Sonnet 5: Fixed a bug in `flag_pages.py` surfaced by the Holyrood 2021 entry below: it unconditionally overwrote `page_rec["status"]`/`["issues"]` from its own coverage/qa_check checks alone every run, silently erasing genuine structural findings the `manifesto-page-repair` skill's in-session audit had just recorded (SNP 2021 p5, UKIP 2021 p0) the moment the deterministic gate happened to pass right after. Added a durable `page_rec["vision_audit"]` field (written by the skill, never written by `flag_pages.py`) holding `discrepancies` in the same shape `qa_audit_vision.py` uses; `flag_pages.py` now reads (never clears) it and folds any discrepancies into its own flagged reasons, so a real finding survives every future re-gate. Backfilled the 3 findings already lost (Alliance 2007 p38, SNP 2021 p5, UKIP 2021 p0) directly into their ledgers and confirmed the fix preserves them on re-gate. Added `tools/transcription-toolkit/list_open_findings.py` to surface every ledger's genuine vs. gate-only-unverified flags in one command instead of re-deriving them from chat history. Separately, discovered only 5 of ~17 repair sessions this week had a surviving Handoff-log/knowledge-log entry — several parallel background agents raced on the same two files and later writers clobbered earlier ones; backfilled the 6 missing entries below from the coordinating session's own records. Also fixed the coverage-baseline gate's two distinct false-positive failure modes flagged in earlier entries: added `--spread-threshold` to `flag_pages.py` (skips the coverage check when deterministic extractors disagree with each other by more than 3x — catches DUP-2007-style partial extractor failure automatically, 50 of its 58 pages), and added `tools/transcription-toolkit/coverage_baseline_allowlist.yaml` (git-tracked, same spirit as `qa_check.py`'s `qa_allowlist.yaml`) for the harder case where all deterministic extractors agree with each other but are still collectively wrong (a corrupted font encoding), which has no statistical signal and needs a human to verify once. Seeded with ScottishGrn 2003 (whole doc) and SNP 2016 (8 specific pages), both solidly backed by a single continuous agent run that audited the full relevant page set and reported it clean. DUP 2007's remaining 8 pages are on weaker footing and the allowlist entry says so precisely rather than overstating it: that repair was split across two agents, the first (57 of 58 candidates) died mid-batch with no saved transcript, so a completed batch-level audit for those pages is unconfirmed; what's actually verified is qa_check clean on all 58 (exhaustive, mechanical), a sample spot-checked against images, and the same extractor-disagreement signature already confirmed on page 2. Verified against all three motivating documents (58→0, 17→0, and SNP 2016's 8 pages only) and confirmed normal manifestos are unaffected, then re-ran `flag_pages.py` across every existing ledger to refresh `flagged_pages.json`.
- 2026-07-20 — Claude Sonnet 5: Resumed the Holyrood 2021 tier-2 batch (11 manifestos, excl. Alba — confirmed untouched via file mtimes) after a session restart. SNP: 3 pages repaired (5, 14, 74) — page 5 is a format-selector icon row missing the decorative glyph text baked into the icons ("BIG TEXT", "PLAIN TEXT", "GLA"), though the 7 real captions underneath are all correct; page 14 is a stripped-boilerplate false positive. UKIP: 1 page repaired (0) — a photo caption rendered italic where the source is plain, non-italic text. These two were the discrepancies that exposed the `flag_pages.py` clobbering bug fixed in the entry above — correctly recorded `needs-review` by the audit, then silently reset to `reviewed` by the very next gate run. Other 9 ledgers (allforunity, cooperative, isp, scottishcon, scottishfamily, scottishgrn, scottishlab, scottishlibdem, scottishlibertarian) needed only the finalize step; all clean except scottishfamily (p48) and scottishgrn (p13), both pre-existing documented flags. Did not touch `manifestos/`.
- 2026-07-20 — Claude Sonnet 5: Ran tier-2 repair on all 10 Holyrood 2016 work dirs. 23 pages repaired across scottishgrn (3) and scottishlab (9) with zero audit discrepancies. SNP: 11 pages repaired, including a genuine degenerate hallucination loop (page 27, ending in gibberish) and genuine missing content (page 75), both fixed; its other 8 repaired pages still gate-flag "coverage high" afterward due to a new false-positive pattern — the source PDF's bulleted-list text uses a corrupted/ciphered custom font encoding, so `pdftotext` renders garbage (e.g. `=K]ORRJKRO\KX` for "We will deliver"), independently re-verified against the images as complete and accurate. communist/rise/scottishlibdem/ukip/wep needed only the finalize step and are clean; cooperative and scottishcon's fresh re-gate surfaced 2 new unverified low-coverage flags outside this session's scope, left for adjudication. Did not touch `manifestos/`.
- 2026-07-20 — Claude Sonnet 5: Resumed the Holyrood 1999+2003 tier-2 batch (11 manifestos) after a session restart. 4 ledgers had real repair work: 2003 ScottishGrn (17 of 20 pages, effectively the whole document) verified clean against images but permanently flagged "coverage high" because this PDF's text layer is corrupted on every page (pdftotext/pdfplumber near-garbage) — a whole-document version of the extractor-untrustworthy problem seen in Stormont 2007 DUP below. 2003 ScottishLab (4 pages, 1 residual boilerplate false positive). 2003 ScottishLibDem (3 pages) — found and fixed a genuine `ordering_error` on page 0 (a summary strip placed after the title block instead of before it), re-verified clean. 2003 SNP (3 pages, 2 residual false positives — non-rendered duplicate text layer on a divider page, and markdown-list formatting inflating a 3-column index page's word count). The remaining 7 ledgers (1999 ScottishCon/ScottishGrn/ScottishLibDem/SNP, 2003 BNP/ScottishCon/SSP) needed only the finalize step; the fresh re-gate surfaced new unverified `qa_check` flags outside scope (1999 SNP p3, 2003 BNP p0, 2003 ScottishCon pp 2/4/7/21), left for adjudication. Did not touch `manifestos/`.
- 2026-07-20 — Claude Sonnet 5: Resumed the Holyrood 2011 tier-2 batch (9 manifestos) after a session restart. Checking the ledgers directly (rather than trusting the task description) found every currently-flagged page across all 9 already had a `claude-clean` candidate and recorded reason from before the restart — no image reads needed, just the finalize step. 3 residual flags (bnp p4, scottishcon p2, scottishlab p2), all pre-existing, already investigated as legitimate coverage-heuristic false positives on sparse/graphical pages. Did not touch `manifestos/`.
- 2026-07-19 — Claude Sonnet 5: Ran tier-2 repair on the other 11 Stormont 2007 work dirs (DUP handled separately, see entry below, due to its size). 13 pages repaired across 6 manifestos: Alliance (4 pages) had one genuine finding, page 38's missing bold caption below a section-divider graphic — correctly left `needs-review` (later backfilled into the durable `vision_audit` field, see the 2026-07-20 entry above). GPNI (3), RSF (2), SDLP (2, one page's "; and" connectors confirmed legitimate), SEA (1), Workers' Party IE (1) rounded out the repairs. NICON, PUP, Sinn Féin, UKUP, UUP needed only the finalize step. Did not touch `manifestos/`. Next step: finalize (Phase 4) into `manifestos/stormont/2007/<party>/manifesto.md` once DUP's flag is adjudicated too.
- 2026-07-19 — Claude Sonnet 5: Ran tier-2 repair on all 11 Stormont 2003 work dirs, resumed after a prior session was killed by a usage limit — only UUP's page 15 (back-cover slogan/logo/imprint) was unrepaired; the other 10 already had every flagged page's candidate written. 7 of 11 manifestos are fully clean at the gate; the rest (Alliance, PUP, SDLP, Sinn Féin) are confirmed false positives — bullet/dash punctuation and dot-leader formatting inflating the coverage heuristic, letter-spaced stylized cover text, and a repeating running side-tab header correctly stripped as boilerplate (Sinn Féin, 4 pages). Did not touch `manifestos/`. Next step: finalize (Phase 4) into `manifestos/stormont/2003/<party>/manifesto.md`.
- 2026-07-20 — Claude Sonnet 5: Picked up the Holyrood 2026 tier-2 repair batch (12 work dirs) after a prior session was killed by a session restart, working from the caller's own re-verified list of still-flagged pages. Repaired from scratch (steps 1-7, all passed the in-session structural audit clean): scottishlibdem (1 page — a purely decorative icon-only page with no real text, correct output is empty), snp (14 pages — fixed a hallucinated/duplicated cover, a hallucinated quote page that dropped the real "Scotland's Right to Decide" heading in favour of poster-in-photo text, several dropped section headings, and a missing back-cover imprint line; several others were already fully accurate and just false-positive-flagged on stripped boilerplate), sovereignty (3 pages — a markdown-table-inflated contents page, a dropped left column on the Jim Fairlie foreword, and an entirely dropped left column on Community & Housing), ssp (2 pages — both missing the party-name title/tagline), workersparty (3 pages — one missing the "Building a new working class politics in Britain" tagline, one missing the entire "Join a Campaigning Party" block, one already accurate). Also found `manifestos__holyrood__2026__scottishlab__manifesto` had 16 pages sitting at `status: pending-audit` (claude-clean candidates already written pre-restart, but never audited) rather than the "nothing left to repair" the caller expected — ran the independent audit pass on all 16 (all section-divider pages with a heading + optional pull-quote); all clean, no discrepancies, now `reviewed`. Ran the requested finalize step (`repair_manifestos_gemini.py --reassemble-only` + `flag_pages.py`) on all 12 ledgers. cooperative, scottishcon, scottishlibertarian, scottishlibdem, and sovereignty came back at 0 flags. The rest still gate-flag post-finalize — all confirmed false positives on spot re-check against the page images: snp (9 pages, all the short quote/heading pages I just repaired) and scottishlab (14 pages, all 14 of the just-audited section dividers) trip "coverage low" purely because the correctly-stripped running header/footer boilerplate is a large fraction of the deterministic candidate's word count on such short pages; ssp p19 and workersparty p2 are the same effect on a back-cover and a title page; isp (5 pages) and reform (1 page) and scottishgrn (2 pages) are pre-existing flags from before this session, already individually audited and left as legitimate coverage/qa_check flags per the caller's own prior characterization. Did not touch `manifestos/`. Next step: finalize (Phase 4) all 12 Holyrood 2026 drafts into `manifestos/holyrood/2026/<party>/manifesto.md`, adjudicating the residual false-positive flags above as accepted.
- 2026-07-20 — Claude Sonnet 5: Picked up the Holyrood 2007 tier-2 repair batch (10 work dirs) after a prior session was killed by a session restart. `manifestos__holyrood__2007__bnp__manifesto` was the only ledger with unrepaired flagged pages (10: page_index 0,2,11,14,17,20,22,25,29,31) — 5 already had `claude-clean` candidates written pre-restart but not yet reflected in `ledger.json` (verified each against its page image and the deterministic candidate, all accurate); wrote the remaining 5 fresh from the images. All 10 passed the in-session structural audit clean and are now `status: reviewed`. For the other 9 ledgers (scottishchristian, scottishcon, scottishgrn, scottishlab, scottishlibdem, snp, solidarity, ssp, ukip), ran the requested finalize-only step (`repair_manifestos_gemini.py --reassemble-only` + `flag_pages.py`) with no image reads — 6 came back clean (0 flagged); scottishlab (pages 51, 74), scottishlibdem (page 0), and snp (page 75) still gate-flag on coverage-low even though their `claude-clean` candidates were already correct. Spot-checked each against its page image: all three are false positives — scottishlab p51 is a section-divider page where `pdftotext` bleeds in unrelated running-header text ("SAFER FUTURES HEALTHIER COMMUNITIES") not actually present on the page; scottishlab p74 is a narrow miss (0.82 ratio) purely from correctly-stripped footer boilerplate; scottishlibdem p0 has a hidden/invisible text layer (`SCOTTISH LIBERAL DEMOCRATS` + a URL) not rendered anywhere on the page; snp p75 is mostly a correctly-stripped vertical printer's-imprint sidebar, and its claude-clean also fixes a genuine column reading-order bug in `pdftotext` (VOTE SNP/DONATE/JOIN IN reversed vs the image's true left-to-right order). BNP's cover page (page_index 0) also re-flags post-finalize for the same reason (a hidden `www.bnp.org.uk` text-layer fragment plus single-letter-per-line fragments of the diagonal "putting local people first" design text, neither visible in the rendered page). Did not touch `manifestos/`. Next step: finalize (Phase 4) all 10 Holyrood 2007 drafts into `manifestos/holyrood/2007/<party>/manifesto.md`, adjudicating the 4 residual false-positive flags above as accepted.
- 2026-07-19 — Claude Sonnet 5: Finished the tier-2 repair of `work/manifestos__stormont__2007__dup__manifesto` (64-page DUP 2007 Stormont manifesto) picked up mid-batch after a prior session was killed by a usage limit; 57/58 flagged pages already had a `claude-clean` candidate, only page_index 62 ("Delivering an Unrivalled Constituency Service") was missing one — repaired it (two-column body text, correct linear left-then-right order, page-number/logo boilerplate stripped) and it passed the in-session structural audit clean. All 58 originally-flagged pages now have `claude-clean` selected and reassembled into `draft.md`. Re-running `flag_pages.py` re-flags all 58 as "coverage high" — this is a **manifesto-wide false positive specific to this source PDF**: 3 of its 5 deterministic extractors (`pdftotext-raw`, `pdfplumber`, `pdfplumber-layout`) catastrophically undercount words on nearly every page (e.g. page 2: 51-55 words vs the true ~389, confirmed by `pdftotext`/`pdftotext-layout`), so the gate's median-of-deterministic-extractors baseline sits far too low and trips the coverage-high ratio on essentially every substantial-text page even though the actual repaired content is correct (spot-checked several pages directly against the page images). No `qa_check` errors/warnings triggered on any page — only the coverage heuristic. Did not touch `manifestos/`; `manifestos/london/2024/sdp/manifesto.audit.json` and other untouched working-tree files are unrelated to this task. Next step: this manifesto's `flagged_pages.json` gate is not usable as a real signal without recalibrating the baseline for PDFs where `pdfplumber`/`pdftotext-raw` degenerate this badly (worth a `flag_pages.py` follow-up — e.g. fall back to `pdftotext`/`pdftotext-layout` alone when the spread between deterministic candidates is this extreme) — then finalize (Phase 4) into `manifestos/stormont/2007/dup/manifesto.md`.
- 2026-07-19 — Claude Sonnet 5: Ran tier-2 `manifesto-page-repair` skill on all 11 NI Assembly/Stormont 2016 work dirs (alliance, dup, gpni, nicon, pup, sdlp, sinnfein, tuv, ukip, uup, workerspartyie). Repaired 22 flagged pages across 8 manifestos (pup, ukip, workerspartyie had 0 flagged, confirmed still clean). Real defects fixed: a fully-hallucinated cover page (Sinn Féin — vlm-clean produced 3,483 words of repeated text + garbage tokens instead of ~20 real words), a missing ~180-word text block/column (SDLP "Third Level Education" section on the Vibrant Economy spread), 3 missing candidate names and a collapsed contents-page heading hierarchy (UUP), and misread proper nouns (DUP: "Altnagelvin" → "Athalganey", "callous murder" → "collars number"; "Greenisland"/"Newtownabbey" misspelled). Post-repair gate: alliance, nicon, pup, sdlp, tuv, ukip, workerspartyie at 0 flags; dup/gpni/sinnfein/uup each have 1-3 pages still flagged as confirmed false positives (back-cover boilerplate — social links/printer's imprint — intentionally stripped per the skill's rule, or markdown list/table punctuation inflating the coverage-heuristic word count on dense contents/candidate-list pages) — all individually re-verified correct against the page images during the audit pass. Did not touch `manifestos/`. Also noted `tools/transcription-toolkit/CLAUDE.md` is stale relative to the current no-API-key pipeline (see knowledge/log.md 2026-07-19 entry) — worth reconciling. Next step: finalize these 11 drafts (Phase 4) into `manifestos/stormont/2016/<party>/manifesto.md`.
- 2026-07-19 — Claude Sonnet 5: Ran tier-2 `manifesto-page-repair` skill on all 9 NI Assembly/Stormont 2011 work dirs (alliance, dup, gpni, pup, sdlp, sinnfein, tuv, uup, workerspartyie). Repaired 22 flagged pages total (real fixes: missing columns, duplicated blocks, wrong reading order); pup/uup had 0 flagged and were confirmed still clean. Post-repair gate: gpni, sinnfein, workerspartyie, uup, pup at 0 flags; alliance/dup/sdlp/tuv each have 1-2 pages still flagged as expected false positives (markdown-table pipe chars or graphical/infographic content inflating the deterministic word-count baseline, or a corrupted/duplicated PDF text layer on a back-cover page) — all individually verified correct against the page images. Did not touch `manifestos/`; draft.md files only reassembled inside each work dir. Next step for whoever picks this up: finalize these 9 drafts (Phase 4) into `manifestos/stormont/2011/<party>/manifesto.md`.
- 2026-07-18 — Claude Fable 5 (Cowork): Removed the API key from the transcription pipeline for future batches. `repair_manifestos_gemini.py` is now backend-agnostic and defaults to a local OCR VLM (DeepSeek-OCR 8-bit MLX via LM Studio, `--backend local --mode ocr`, candidate `vlm-clean`); added `flag_pages.py` deterministic QA gate (word coverage + per-page qa_check) producing `flagged_pages.json`; added `.claude/skills/manifesto-page-repair` so a Claude Code session repairs flagged pages in-session (candidate `claude-clean`, no API). `batch_repair_london.py` forwards extra args. Setup/go-no-go: `tools/transcription-toolkit/LOCAL_SETUP.md`.
- 2026-07-18 — Claude Fable 5 (Cowork): VALIDATED on Alan's M1 Max with LM Studio + DeepSeek-OCR-8bit on the 2005 Veritas manifesto — all 10 pages transcribed locally, quality matched/beat the deterministic candidates. Fixed three issues surfaced by that run: `work_dir_for` absolute-slug bug (out-of-repo PDFs wrote to `/`); `flag_pages.py` now uses the **median** deterministic word count as the coverage baseline, not max (pdfplumber ~doubles the count on multi-column pages → was 9/10 false positives, now 0); and confirmed operators must use `/opt/homebrew/bin/python3.12` (default `python3` is pre-3.10 and fails on the bundled pdfplumber). Remaining harmless flag: imprint-footer bullet separators can raise a B2/R2 qa_check warning on final pages. Pipeline is ready for the next batch.
- 2026-07-18 — Cursor Grok: Rationalised London election ids to year-only (`/devolved/london/2000`, `/manifesto/london/2000/livingstone`); 301s from legacy `gla-`/`glc-`/`lcc-` paths.
- 2026-07-18 — Cursor Grok: Wired all transcribed GLA mayoral markdown into SPA text routes. Stamped manifesto `id` (= folder slug) on GLA JSON; cards/viewer key off that (Livingstone → `/manifesto/london/2000/livingstone`); indexed GLA text routes; middleware accepts 4-segment manifesto URLs. Independents no longer collide on `/independent`.
- 2026-07-17 — Gemini 3.5 Flash: Completed full visual repair, cleanup, and finalization for all 47 London devolved election manifestos across all 7 available years (2024, 2021, 2016, 2012, 2008, 2004, and 2000). Set up automation batch script `batch_london_manifestos.py` and page-level visual repair helper `repair_manifestos_gemini.py` using `gemini-2.5-flash` API. Resolved 429 API credit issues, processed and visually verified all 47 manifestos, added canonical H1 titles and metadata frontmatter, finalized and copied them to the repo folders, and successfully rebuilt sitemaps, PDF sizes, site indexes, and latest-additions.
- 2026-07-16 — Gemini 3.5 Flash: Batch converted all 147 London devolved election PDFs under original documents to markdown versions using Microsoft's MarkItDown, saving them under 'Markdown versions/London' maintaining the mirrored subdirectory structure. Resolved unreadable outputs by applying a custom font-based shift decoder (+31/+32 character offset) to rebuild the scrambled 2004 Simon Hughes PDF, and cleaned up bullet/spacer CIDs in the 2008 Boris Johnson PDF. Transcribed the 6 scanned/image-only PDFs from the 2024 London election (including Susan Hall and Brian Rose) using Gemini's OCR vision endpoint.
- 2026-06-29 — Codex: added `tools/transcription-toolkit/transcribe_pipeline.py`
  as a human-gated page-ledger layer for new transcriptions, retrospective audits,
  conservative repair drafts, and batch audit reports. The workflow now explicitly
  retains Contents/Table of Contents sections while stripping page numbers.
- 2026-06-29 — Codex: extended the pipeline with `--source-text` and
  `batch-audit-text` for authoritative historical text sources such as the local
  Iain Dale Labour splits. Ran a no-sidecar Labour 1945-1992 pilot: 14 pairs found,
  6 passed, 8 need review. Ran a no-sidecar 12-file PDF pilot across 2005-2024;
  all were conservatively queued for human review, mainly due to complex layouts,
  TOC cleanup, heading verification, and reading-order warnings.
