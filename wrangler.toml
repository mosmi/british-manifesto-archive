# Static site — deployed via Cloudflare Workers Git integration.
# Dashboard should keep: Build command = (empty), Deploy command = npx wrangler deploy
#
# If you prefer Cloudflare Pages instead, create a new *Pages* project
# (Workers & Pages → Create → Pages tab), not a Workers project.
name = "british-manifesto-archive"
compatibility_date = "2024-09-23"

[assets]
directory = "./"
